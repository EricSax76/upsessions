/// Filter options for rehearsal lists.
enum RehearsalFilter {
  /// Shows only upcoming rehearsals (future dates).
  upcoming,
  
  /// Shows all rehearsals regardless of date.
  all,
  
  /// Shows only past rehearsals.
  past,
}
